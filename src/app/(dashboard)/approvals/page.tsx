"use client";
import { Check, Clock3, RotateCcw } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/Button";
import { PriorityBadge, StatusBadge } from "@/components/tasks/TaskBadges";
import { demoTasks } from "@/data/demo";
export default function ApprovalsPage(){const tasks=demoTasks.filter(t=>t.status==="Submitted for Review");return <div className="page-wrap"><div className="mb-7"><h1 className="page-heading">Approvals</h1><p className="page-subtitle">Review submitted work and provide a clear next step.</p></div><div className="space-y-4">{tasks.map(task=><article key={task.id} className="card p-5"><div className="flex flex-col justify-between gap-5 lg:flex-row lg:items-center"><div><div className="mb-2 flex items-center gap-2"><span className="text-[10px] font-bold text-[var(--muted)]">{task.taskNumber}</span><StatusBadge status={task.status}/><PriorityBadge priority={task.priority}/></div><h2 className="text-sm font-extrabold">{task.title}</h2><p className="mt-1 text-xs text-[var(--muted)]">Submitted by {task.assignedToDetails?.[0]?.name} · {task.projectName}</p></div><div className="flex gap-2"><Button variant="secondary" onClick={()=>toast.info("Changes requested",{description:"The assignee has been notified."})}><RotateCcw className="size-4"/>Request changes</Button><Button onClick={()=>toast.success("Task approved and completed")}><Check className="size-4"/>Approve</Button></div></div></article>)}{tasks.length===0&&<div className="card flex min-h-72 flex-col items-center justify-center text-center"><Clock3 className="mb-3 size-7 text-[#0796c2]"/><h2 className="font-bold">Nothing awaiting review</h2></div>}</div></div>}
