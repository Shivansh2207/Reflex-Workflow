"use client";
import { Grid2X2, List, Search, SlidersHorizontal, UserRoundCheck } from "lucide-react";
import { useMemo, useState } from "react";
import { EmployeeCard } from "@/components/employees/EmployeeCard";
import { Button } from "@/components/ui/Button";
import { demoEmployees } from "@/data/demo";
export default function EmployeesPage(){const [search,setSearch]=useState("");const [department,setDepartment]=useState("All departments");const [availability,setAvailability]=useState("All availability");const employees=useMemo(()=>demoEmployees.filter(item=>(!search||`${item.fullName} ${item.email} ${item.designation} ${item.employeeId}`.toLowerCase().includes(search.toLowerCase()))&&(department==="All departments"||item.departmentName===department)&&(availability==="All availability"||item.availabilityStatus===availability)),[search,department,availability]);return <div className="page-wrap"><div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="page-heading">Team directory</h1><p className="page-subtitle">Find teammates, understand availability, and assign work quickly.</p></div><div className="flex items-center gap-2 text-xs text-[var(--muted)]"><UserRoundCheck className="size-4 text-emerald-500"/><strong className="text-[var(--foreground)]">{demoEmployees.filter(e=>e.availabilityStatus==="Available").length}</strong> available now</div></div><div className="card mb-6 p-3"><div className="flex flex-col gap-3 lg:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 size-4 text-[var(--muted)]"/><input className="control h-10 min-h-10 pl-10" placeholder="Search by name, email, role, or employee ID…" value={search} onChange={e=>setSearch(e.target.value)}/></div><select className="control h-10 min-h-10 w-full text-xs lg:w-44" value={department} onChange={e=>setDepartment(e.target.value)}><option>All departments</option>{[...new Set(demoEmployees.map(e=>e.departmentName))].map(value=><option key={value}>{value}</option>)}</select><select className="control h-10 min-h-10 w-full text-xs lg:w-44" value={availability} onChange={e=>setAvailability(e.target.value)}><option>All availability</option>{[...new Set(demoEmployees.map(e=>e.availabilityStatus))].map(value=><option key={value}>{value}</option>)}</select><Button variant="secondary" size="icon"><SlidersHorizontal className="size-4"/></Button><div className="hidden items-center rounded-xl bg-[var(--surface-2)] p-1 sm:flex"><button className="flex size-8 items-center justify-center rounded-lg bg-[var(--surface)] text-[#0796c2] shadow-sm"><Grid2X2 className="size-4"/></button><button className="flex size-8 items-center justify-center rounded-lg text-[var(--muted)]"><List className="size-4"/></button></div></div></div><div className="mb-4 text-xs text-[var(--muted)]"><strong className="text-[var(--foreground)]">{employees.length}</strong> people</div>{employees.length?<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4">{employees.map(employee=><EmployeeCard key={employee.uid} employee={employee}/>)}</div>:<div className="card flex min-h-72 items-center justify-center text-sm text-[var(--muted)]">No teammates match these filters.</div>}</div>}
