"use client";
import { MoreHorizontal, Plus, Search } from "lucide-react";
import { Button } from "@/components/ui/Button";
import { Avatar } from "@/components/ui/Avatar";
import { Badge } from "@/components/ui/Badge";
import { demoEmployees } from "@/data/demo";
export default function AdminEmployeesPage(){return <div className="page-wrap"><div className="mb-7 flex items-end justify-between"><div><h1 className="page-heading">Employee administration</h1><p className="page-subtitle">Invite employees, manage roles, and control account access.</p></div><Button onClick={()=>alert("Connect Cloud Functions and email delivery to send employee invitations.")}><Plus className="size-4"/>Invite employee</Button></div><div className="card overflow-hidden"><div className="border-b p-4"><div className="relative max-w-sm"><Search className="absolute left-3 top-3 size-4 text-[var(--muted)]"/><input className="control h-10 min-h-10 pl-10" placeholder="Search employees…"/></div></div><div className="divide-y">{demoEmployees.map(employee=><div key={employee.uid} className="grid gap-3 px-5 py-4 md:grid-cols-[1fr_160px_120px_100px_40px] md:items-center"><div className="flex items-center gap-3"><Avatar name={employee.fullName}/><div><div className="text-xs font-bold">{employee.fullName}</div><div className="mt-1 text-[10px] text-[var(--muted)]">{employee.email}</div></div></div><span className="text-xs text-[var(--muted)]">{employee.departmentName}</span><Badge tone={employee.role==="manager"?"purple":"neutral"}>{employee.role}</Badge><Badge tone="green" dot>{employee.accountStatus}</Badge><button className="text-[var(--muted)]"><MoreHorizontal className="size-4"/></button></div>)}</div></div></div>}
