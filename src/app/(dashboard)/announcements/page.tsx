"use client";
import { Megaphone, Paperclip, Pin, Plus } from "lucide-react";
import { format } from "date-fns";
import { Button } from "@/components/ui/Button";
import { Badge } from "@/components/ui/Badge";
import { demoAnnouncements } from "@/data/demo";
import { useAuth } from "@/providers/AuthProvider";
export default function AnnouncementsPage(){const {employee}=useAuth();return <div className="page-wrap"><div className="mb-7 flex items-end justify-between"><div><h1 className="page-heading">Announcements</h1><p className="page-subtitle">Important team and company updates in one place.</p></div>{employee?.role!=="employee"&&<Button onClick={()=>alert("Announcement composer opens for authorized managers in the connected workspace.")}><Plus className="size-4"/>Post update</Button>}</div><div className="mx-auto max-w-3xl space-y-4">{demoAnnouncements.map((item,index)=><article key={item.id} className="card p-5 sm:p-7"><div className="flex gap-4"><div className="flex size-11 shrink-0 items-center justify-center rounded-xl bg-[var(--cyan-soft)] text-[#0796c2]"><Megaphone className="size-5"/></div><div className="min-w-0 flex-1"><div className="mb-2 flex flex-wrap items-center gap-2">{item.isPinned&&<Badge tone="cyan"><Pin className="size-3"/>Pinned</Badge>}<Badge>{item.audience}</Badge><span className="text-[10px] text-[var(--muted)]">{format(new Date(item.createdAt),"d MMM yyyy · h:mm a")}</span></div><h2 className="text-base font-extrabold">{item.title}</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{item.content}</p><div className="mt-5 flex items-center justify-between border-t pt-4 text-[10px] text-[var(--muted)]"><span>Posted by <strong className="text-[var(--foreground)]">{item.authorName}</strong></span>{index===0&&<span className="flex items-center gap-1"><Paperclip className="size-3"/>town-hall-agenda.pdf</span>}</div></div></div></article>)}</div></div>}
